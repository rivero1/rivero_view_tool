require "spec_helper"

describe RiveroViewTool do
  it "has a version number" do
    expect(RiveroViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
